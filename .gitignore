# dependencies
node_modules/

# next
.next/
out/

# env — never commit secrets / local config
.env
.env.local
.env*.local

# prisma — local SQLite databases (migration files ARE committed)
prisma/*.db
prisma/*.db-journal
*.db
*.db-journal

# prisma — generated client (regenerated by `prisma generate`)
generated/

# testing
coverage/

# misc
.DS_Store
*.log